import 'package:flutter/material.dart';

class SignUpClientPage extends StatelessWidget {
  const SignUpClientPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Sign Up Client'),
      ),
    );
  }
}
