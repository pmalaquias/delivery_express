package com.pmalaquias.deliveryexpress.data.models.enums

/**
 * Enum class representing the availability status of a product.
 *
 * @property AVAILABLE Represents a product status where the product is available.
 * @property UNAVAILABLE Represents a product status where the product is unavailable.
 */
enum class ProductStatus {
    AVAILABLE,
    UNAVAILABLE
}